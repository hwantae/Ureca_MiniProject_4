spring:
  datasource:
    url: jdbc:mysql://localhost:3306/miniproject_4?serverTimezone=Asia/Seoul
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: none
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      platforms: mysql
      schema-locations: classpath:testdata.sql
